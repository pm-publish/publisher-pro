{% set articleCount = 0 %}
{% set cardIndex = 0 %}


<div class="row" data-section="07">
    <div class="col-12">
        <div class="u-generic-topBorder "></div>
        <div class="c-page-head c-page-head-xs">
            <div class="u-heading__large u-heading__large-xs u-pr--6">{{title}}</div>
            {% if linkShow %}
                <a href="{{link}}" class="c-page-head__link">View all</a>
            {% endif %}
        </div>
    </div>
</div>

<div class="row">
    <div class="col">
        <div class="c-cards-container c-cards-container-xs bottom-border">
            <div class="row">
                <div class="col-12 col-lg-9 left-border">
                    <div class="row">
                        <div class="col-12">
                            <div class="c-cards-container c-cards-container-xs bottom-border">
                                {{this.render('../_single-article.twig', {
                                article: articles[articleCount],
                                position: (articleCount+1),
feedBlogGuid: articles[articleCount]['requestedBlog']['guid'],
                                imageSize: {width: 950, height: 675},
                                imageSizes: {
                                    "mobile": {
                                        width: 635, height:450
                                    },
                                    "tablet" : {
                                        width: 950, height:675
                                    }
                                }, 
                                swap : 'false',
                                containerClass: "card-2-mobile card-2-tablet card-7-desktop",
                                timeSuppress: timeSuppress
                                }) | raw}}
                                {% set articleCount = (dev) ? articleCount : articleCount + 1 %}
                            </div>
                        </div>
                        {% for i in 1..3 if articles[articleCount] %}
                            <div class="col-12 col-lg-4 left-border">
                                {{this.render('../_single-article.twig', {
                                article: articles[articleCount],
                                position: (articleCount+1),
feedBlogGuid: articles[articleCount]['requestedBlog']['guid'],
                                imageSize: {width: 459, height: 278},
                                imageSizes: {
                                    "mobile": {
                                        width: 550, height: 333
                                    },
                                    "mobileLrg": {
                                        width: 550, height: 333
                                    },
                                    "tablet": {
                                        width: 703, height: 425
                                    }
                                },
                                swap : 'false',
                                containerClass: "card-2-mobile card-2-tablet card-2-desktop card-2--noDescription
                                card--verticalMulti card--verticalMulti-xs",
                                timeSuppress: timeSuppress
                                }) | raw}}
                                {% set articleCount = (dev) ? articleCount : articleCount + 1 %}
                            </div>
                        {% endfor %}
                    </div>
                </div>
                <div class="col-12 col-lg-3 card-borderBottom__deskVersion left-border">
                    {% for i in 4..6 if articles[articleCount] %}
                        {{this.render('../_single-article.twig', {
                        article: articles[articleCount],
                        position: (articleCount+1),
feedBlogGuid: articles[articleCount]['requestedBlog']['guid'],
                        imageSize: {width: 120, height: 145},
                        imageSizes: {
                            "mobile": {
                                width: 120, height: 145
                            },
                            "mobileLrg": {
                                width: 120, height: 145
                            },
                            "tablet": {
                                width: 120, height: 145
                            }
                        },
                        swap : 'false',
                        containerClass: "card-1-mobile card-1-tablet card-1-desktop",
                        timeSuppress: timeSuppress
                        }) | raw}}
                        {% set articleCount = (dev) ? articleCount : articleCount + 1 %}
                    {% endfor %}

                    {% if adslots[0] is defined %}
                        <div class="mt-2">
                            <div class="j-adslot" id="{{adslots[0]}}" data-adshape="mrec"></div>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>